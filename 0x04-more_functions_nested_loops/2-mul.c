#include "main.h"

/**
 * mul - this multiplies two incoming digits
 * @a: first incoming number
 * @b: second incoming number
 * res: displays the multiplication result
 * Return: returns the multiplied value
 */

int mul(int a, int b)
{
	int res;

	res = (a * b);
	return (res);
}
